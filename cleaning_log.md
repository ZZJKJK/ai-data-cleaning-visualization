# Titanic 数据清洗日志

**原始数据**：`titanic_raw.csv`（891 行 × 15 列，由 `load_data.py` 从 seaborn 内置 titanic 数据集导出）
**清洗脚本**：`clean_data.py`　**输出数据**：`titanic_clean.csv`（784 行 × 11 列）
**质量依据**：`data_quality_report.md`、`titanic_raw_eda_report.md`（探索脚本 `explore.py`）
**分析环境**：Python 3.14.6 ｜ pandas 3.0.3 ｜ numpy 2.4.6
**记录日期**：2026-09-17

---

## 一、清洗目标

1. **缺失值有据可依**：能推断的分组填补，不能推断的只保留「是否缺失」的标记，不用均值/众数编造信息。
2. **剔除冗余与泄漏**：删掉由其它列派生出来的重复列，尤其是与目标列 `survived` 等价的 `alive`。
3. **异常值处理可追溯**：区分「真实极值」和「记录错误」，说明每一步是保留、截断还是变换。
4. **全流程可复现**：脚本、日志、质量报告三者的数字必须一致，任何人重跑 `clean_data.py` 都能得到同一份结果。

## 二、前后对比总览

| 项目 | 清洗前 | 清洗后 |
|------|--------|--------|
| 形状 | 891 × 15 | 784 × 11 |
| 整行重复 | 107 行 | 0 行 |
| 缺失单元 | 869 / 13,365（6.5%） | 0 |
| 缺失列 | age 177、deck 688、embarked 2、embark_town 2 | 无 |
| 新增列 | — | `age_missing`、`has_deck` |
| 删除列 | — | `deck`、`class`、`who`、`adult_male`、`alive`、`embark_town` |
| `fare` 最大值 | 512.33 | 73.20（截断后） |
| 生还率 | 38.38% | 41.20% |

## 三、逐步清洗记录

### 步骤 1｜读取原始数据

- **做法**：`pd.read_csv("titanic_raw.csv")`，确认形状为 891 × 15。
- **理由**：清洗前先固化基线，后续每一步的删改都能与这个基线对上账。
- **结果**：15 列中数值列 6 个（survived、pclass、sibsp、parch、age、fare），类别列 6 个（sex、embarked、class、who、deck、embark_town），布尔列 3 个（adult_male、alone、alive）。

### 步骤 2｜删除整行重复（107 行）⚠️

- **做法**：`df.drop_duplicates()`，行数由 891 降到 784。
- **理由**：表中只剩 15 个字段，`name`、`ticket`、`cabin` 等身份列在 seaborn 版本里已被移除，因此「不同乘客特征完全相同」在理论上无法排除，重复本身也说明这几行没有区分度。
- **结果**：删除 107 行；去重后年龄缺失由 177 降到 106、`deck` 缺失由 688 降到 582（缺失率 77.22% → 74.23%）。
- **影响提示**：生还率从 38.38% 抬到 41.20%，说明这 107 行并非随机分布，**删与不删会改变后续建模的基线**。这一点与质量报告的结论相反，详见第四节的「待确认事项 1」。

### 步骤 3｜`embarked` / `embark_town`：众数填充

- **做法**：两列各用众数 `'S'`（Southampton）填充 2 个缺失值。
- **理由**：缺失仅 2 行（0.22%），两列同源且映射完全自洽（S=Southampton、C=Cherbourg、Q=Queenstown），任何合理选择的影响都可忽略；用众数填充后两列映射关系保持 0 处冲突。
- **结果**：填补 2 行，取值仍只有 S / C / Q 三类。

### 步骤 4｜`age`：分组中位数填充 + 保留缺失标记

- **做法**：
  1. 先新增二值指示列 `age_missing`（缺失 = 1），把「缺失」本身当作信息保留；
  2. 按 `pclass` × `sex` 分组，用组内中位数填充；
  3. 若某组全为空，再用全局中位数兜底。
- **理由**：`age` 缺失率 19.87%，且缺失并非随机——3 等舱 27.7%、1 等舱 13.9%、2 等舱 6.0%，与舱位强相关。全局均值会把舱位间的年龄差异抹平，分组中位数更贴近真实分布；同时舱位和性别本身也与生还率强相关，分组填补不会引入系统性偏移。
- **结果**：填补 106 行；6 个分组（pclass 1/2/3 × female/male）都有观测值（84/93、101/121、71/73、88/92、101/127、233/278），兜底分支实际未触发。清洗后 `age` 取值 0.42 ~ 80.0 岁。

### 步骤 5｜`deck`：转 0/1 指示特征后删除原列

- **做法**：新增 `has_deck = df["deck"].notnull().astype(int)`，然后删除 `deck` 原列。
- **理由**：`deck` 缺失 688 行（77.22%），填补等于凭空编造 77% 的信息。但缺失不是数据丢失，而是原始记录里只有部分乘客登记了舱位号——1 等舱缺失率 18.98%、2 等舱 91.30%、3 等舱 97.56%，缺失模式本身与舱位/票等级高度相关，因此用 0/1 指示列把「有没有登记甲板」这一信息保留下来。
- **结果**：`has_deck = 0` 共 582 行、`has_deck = 1` 共 202 行；`deck` 原名与原值不再进入建模。

### 步骤 6｜删除冗余列与标签泄漏列

- **做法**：删除 `class`、`who`、`adult_male`、`alive`、`embark_town` 五列。
- **理由**：这 5 列都能由其余列完全复现，实测冲突行数均为 0，保留只会带来多重共线性：
  - `class` = `pclass` 的名称版（First/Second/Third）；
  - `alive` = `survived` 的文本版（no/yes），**与建模目标完全等价，属标签泄漏，必须删除**，否则模型指标虚高；
  - `embark_town` = `embarked` 的城市全称版，两列留一即可；
  - `who` = `sex` + `age`（age ≤ 15 定义为 child）；
  - `adult_male` = `sex` + `age`（True 537 行 vs `sex = male` 577 行，差的 40 行是 0.42~15 岁的未成年男性）。
- **结果**：列数由 15 降到 11（含新增的 `age_missing`、`has_deck`）。

### 步骤 7｜类型转换

- **做法**：把 `survived`、`pclass`、`sex`、`embarked`、`alone` 五列转换为 `category`。
- **理由**：这些都是取值有限的离散量（分别只有 2 / 3 / 2 / 3 / 2 个取值），语义上是类别而不是连续数值；转成 `category` 后 one-hot 或有序编码都更直接，也能避免被误当作数值参与运算（例如 `pclass` 的 1 < 2 < 3 不能当线性量用）。
- **结果与注意点**：CSV 不保存 `dtype`，所以 `titanic_clean.csv` 读回来实际是 `int64` / `str` / `bool`，**类别转换没有落盘**。建模时需在读入后重新执行 `astype("category")`，见第四节「待确认事项 4」。

### 步骤 8｜异常值处理

- **`fare`：IQR 截断**
  - **做法**：在去重后的数据上计算 Q1 = 8.05、Q3 = 34.11、IQR = 26.06，上界 = 73.20，用 `clip(lower, upper)` 把超出上界的值压到边界。
  - **理由**：`fare` 长尾极重（原始偏度 4.79、峰度 33.4，最大值 512.33 而 75% 分位数仅 31.00），极端值会主导线性模型和距离类算法，截断后分布更紧凑。
  - **结果**：102 个高票价被压到 73.20，最大值由 512.33 变为 73.20；`fare = 0` 的行由 15 降到 10（去重后）。**注意**：截断会抹掉高价票的真实差异，与质量报告建议的 `log1p` 变换口径不同，见第四节「待确认事项 2」。
  - **附带说明**：截断边界依赖是否先去重（原始数据算出的上界是 65.63，去重后是 73.20），本例因为先执行了步骤 2，所以用的是 73.20。
- **`age`：业务规则体检**
  - **做法**：检查 `age < 0` 或 `age > 100`。
  - **理由**：年龄的极端值多是录入错误，用规则判断比统计方法更可靠。
  - **结果**：0 条异常；最小值 0.42 岁、最大值 80 岁都是真实乘客分布，予以保留。
- **`sibsp` / `parch`：不使用 IQR**
  - **理由**：这两列的 Q1 = Q3 = 0、IQR = 0，IQR 上界会被算成 0，于是 46 / 213 个正常取值全被误判为「异常」。这是方法失效，不是数据错误，本次不做任何剔除。

### 步骤 9｜保存与一致性校验

- **做法**：`df.to_csv("titanic_clean.csv", index=False)`。
- **结果**：输出 784 行 × 11 列，缺失单元数为 0；列顺序为 `survived, pclass, sex, age, sibsp, parch, fare, embarked, alone, age_missing, has_deck`。
- **校验**：已按 `clean_data.py` 的流程重新独立复算一遍，与保存的文件逐行逐列完全一致（列顺序相同、`fare` 数值逐行相同），可确认该 CSV 就是当前脚本的产物。

## 四、与《数据质量报告》的对照：4 处待确认事项

清洗日志记录的是**脚本实际执行的动作**。其中几处与 `data_quality_report.md` 的处理建议口径不同，需要你确认以哪一口径为准：

| # | 项目 | 质量报告的建议 | 脚本实际执行 | 影响 |
|---|------|----------------|--------------|------|
| 1 | 重复行 | 明确写「**保留，不删除（已确认）**」，并给出理由：107 条重复是身份列被删除后的特征撞车；160 行重复行中 93 行（58.1%）`age` 缺失、158 行 `deck` 缺失；剔除 `age` 缺失后重复从 107 降到 36，再剔除 `deck` 缺失只剩 1 行 | `drop_duplicates()`，删除全部 107 行 | 少了 107 个样本，生还率从 38.38% 变为 41.20%。若这些行确为不同乘客，则属于信息损失 |
| 2 | `fare` 长尾 | 建议 `log1p` 变换或分箱，并给 `fare = 0` 单独加指示列，即改变分布、保留全部原始信息 | IQR 截断到 73.20，102 个值被压平，`fare = 0` 未加标记 | 高票价乘客之间的差异被抹平，1 等舱的价格信号变弱；若后续用树模型，截断的收益本身也有限 |
| 3 | `alone` 列 | 建议删除（`alone` = `sibsp` + `parch` 派生），并另建 `family_size` 特征 | 保留 `alone`（转为 `category`，落盘为 bool） | 与后续的 `family_size` 会重复表达同一信息，引入共线性 |
| 4 | 类型转换 | 建议建模前把类别列转 `category` 或 one-hot | 脚本转完立即 `to_csv`，`dtype` 未落盘 | `titanic_clean.csv` 读回来是 `int64` / `str` / `bool`，建模前需重新转换 |

另外一处仅为命名差异：质量报告建议的指示列名为 `deck_recorded`，脚本实现为 `has_deck`，含义完全相同，建议统一命名口径。

## 五、结论与下一步

清洗后的 `titanic_clean.csv` 满足「无缺失、无重复、无冗余、无标签泄漏」四项要求，11 列全部可用，可直接进入特征工程。当前的 11 列是：

`survived`（目标）、`pclass`、`sex`、`age`、`sibsp`、`parch`、`fare`、`embarked`、`alone`、`age_missing`、`has_deck`。

建议的下一步：

1. **先拍板第四节的两处口径**（重复行、`fare` 处理），因为它们直接改变样本量与分布，越早确定越省事；
2. 建 `family_size = sibsp + parch + 1` 并分箱（0 / 1–2 / 3+），同时决定 `alone` 的去留；
3. 若接受保留重复行的口径，另存一份去重版本，与保留版本分别建模做敏感性对比；
4. 对 `fare` 补充 `log1p` 版本与 `fare_zero` 指示列，与截断版本对比分布和模型表现；
5. 建模前重新执行类别转换与 one-hot，并核对 `age_missing`、`has_deck` 与 `survived` 的关系。

## 附：复现步骤

```bash
python load_data.py   # 从 seaborn 导出 titanic_raw.csv（891 × 15）
python explore.py     # 生成缺失值、异常值图表到 figures/
python clean_data.py  # 按上述 9 步清洗，输出 titanic_clean.csv（784 × 11）
```
